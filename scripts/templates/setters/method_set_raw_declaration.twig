
                /**
                * @brief Set {{field.name}}
                * @param uint8_t* {{field.name_lower_camel}}
                * @param size_t size
                * @return void
                */
                void set{{field.name_camel}}(uint8_t* data, size_t size);
                